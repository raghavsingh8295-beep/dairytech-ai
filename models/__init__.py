"""Entity models package.

Each model module must be imported here so that `Base.metadata` sees every
table before `init_database()` runs. Empty for now — populated as each
feature module (Authentication, Farm, Cow, ...) is built.
"""
