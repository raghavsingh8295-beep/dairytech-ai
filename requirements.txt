# --- Core (Module 0: Foundation) ---
customtkinter==5.2.2
SQLAlchemy==2.0.43
python-dotenv==1.1.1
Pillow==11.3.0

# --- Module 1: Authentication ---
bcrypt==4.3.0

# --- Module 3: Cow Management ---
qrcode==8.2

# --- Mobile API backend ---
fastapi==0.139.2
uvicorn[standard]==0.51.0
pyjwt==2.13.0
pydantic==2.13.4
python-multipart==0.0.32

# --- Cloud deployment (Postgres, since the free-tier host's disk is ephemeral) ---
psycopg[binary]==3.2.10

# --- Photo storage (farm/cow photos) — same ephemeral-disk reason as
# Postgres above, just not caught until real photo uploads started
# vanishing after each deploy. See utils/file_storage.py. ---
cloudinary==1.46.2

# --- AI Dairy Assistant (Phase 1): local embeddings (ONNX Runtime, not the
# full PyTorch/sentence-transformers stack — that measured ~477MB resident
# just to load the model and OOM-killed the API on Render's free 512MB
# tier the first time it was actually used; onnxruntime measures ~280MB
# for the same model, verified to preserve embedding quality within 0.997
# cosine similarity of the fp32 original — see assistant/embedding.py and
# scripts/export_embedding_model.py) + pgvector retrieval + Claude
# generation. See requirements-ingest.txt for the separate, local-machine-
# only PDF extraction dependency (PyMuPDF, AGPL-licensed — deliberately
# never installed on the deployed server). ---
onnxruntime==1.28.0
tokenizers==0.22.2
numpy==2.5.1
pgvector==0.5.0
anthropic==0.120.2

# --- Added in later modules (uncomment as each module is built) ---
# alembic              # database migrations
# matplotlib           # data visualization module
# reportlab            # PDF reports module
# openpyxl             # Excel reports module
# scikit-learn         # AI module (predictions, health scoring)
# pandas               # AI module / analytics

# --- Packaging (build-time only) ---
# pyinstaller
